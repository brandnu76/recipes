name: Add logo.webp
on:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  add-logo:
    runs-on: ubuntu-latest
    steps:
      - name: Leave the Lowcountry lockup in place
        run: echo "logo.webp is the Carroll Lowcountry Table lockup and is not rebuilt from the old smokehouse base64 parts."
